package com.emergencyrouter.osm.osm;

import com.emergencyrouter.model.Graph;

public final class OpenStreetMapLoader {

    public Graph load(String filePath) {

        Graph graph = new Graph();

        // هنا ستقرأ ملف OSM
        // وتحوّله إلى Nodes + Edges

        return graph;
    }
}